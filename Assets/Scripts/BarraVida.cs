using UnityEngine;

public class BarraVida : MonoBehaviour
{
    //aqui se implementara con el plyaer

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
